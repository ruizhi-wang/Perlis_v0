Set up own branch

1) Create local branch

Syntax:
$ git checkout -b <BRANCH_NAME>

Command:
$ git checkout -b dev-lukas

2) Push branch to remote

Syntax:
$ git push <REMOTE_NAME> <BRANCH_NAME>

Command:
$ git push origin dev-lukas